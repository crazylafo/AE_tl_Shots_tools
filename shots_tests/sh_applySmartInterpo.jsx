// js inputvar proj = app.project;function getSelectedShots(){    var shotsSelected = [];    for (var i =1; i<proj.numItems; i++){        if (proj.item(i).selected == true &&        	proj.item(i) instanceof CompItem){            	shotClass = {};            	shotClass.index= i;            	shotsSelected.push (shotClass)            	}        	}    return shotsSelected    }    function applySmartInterpo (compList){     for (var i =0; i<compList.length; i++){         var num = parseInt(compList[i].index);    	proj.item(num).ramPreviewTest("",1,"")    	var comp = app.project.activeItem;    	try{    		comp.layer(1).frameBlendingType = FrameBlendingType.PIXEL_MOTION;    	}    	catch(e){    		continue    	}    }}var shotsList = getSelectedShots();applySmartInterpo (shotsList)