// if inpoint a = inpoint b then put a before b //delimit layers to move var arrayLayerTarget = [1,8]; //group layer avar arrayLayerRef = [14, 319]; //group layer bvar comp = app.project.activeItem;if (comp){      app.beginUndoGroup ("shots");    for (var i=  arrayLayerTarget [0]; i<=arrayLayerTarget [1]; i++){        for (var j= arrayLayerRef [0]; j<arrayLayerRef [1]; j++){            indexI =arrayLayerTarget [0];            if( comp.layer (indexI).inPoint ==  comp.layer (j).inPoint ){                comp.layer(arrayLayerTarget [0]).moveBefore(comp.layer(j));                break;                }            }        }    app.endUndoGroup();    }