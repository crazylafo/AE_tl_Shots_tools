var comp = app.project.activeItem;function shotNameNum (shNum){     if (parseInt(shNum)<100) {            shNum= "00"+(shNum);         }     else if (shNum<1000) {         shNum= "0"+(shNum);         }     return shNum    }if (comp){    var nl = comp.numLayers;    var shName = "SHOTS";    var  shNum= 10;     app.beginUndoGroup ("shots");    for (var  i = nl; i>5; i--) {        // precompose the item                var shotMat =  shotNameNum (shNum);        var currentPrecompName = shName+"_"+ shotMat+"_001" ;        var newLayerPreComp =app.project.activeItem.layers.precompose([i], currentPrecompName, false)         shNum +=10;        }    app.endUndoGroup();    }